{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Create new User</h1>
                {% for item in user.rollen %}
                    <li>{{ item }}</li>
                {% endfor %}
                {{ include('user/_form.html.twig') }}

                <a href="{{ path('user_index') }}">back to list</a>
            </div>
        </div>
    </div>
{% endblock %}
